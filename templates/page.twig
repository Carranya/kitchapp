<div class='inline-block w-96 border border-pink-300 rounded-xl text-left p-3 m-5 bg-pink-300 shadow-xl'>
    <h1 class='text-center text-2xl font-bold underline'>Einkaufsliste</h1>
        {% set step = 0 %}
        {% for test in tests %}
            {% if step == 0 %}
                <div class='p-3 bg-pink-200 flex justify-between'>
                    {{ test }}
                    <span>2 Stück</span>
                </div>
                {% set step = step + 1 %}
            {% else %}
                <div class='p-3 bg-pink-100'>
                    {{ test }}
                </div>
                {% set step = step - 1 %}
            {% endif %}
        {% endfor %}
</div>